[build]
# Default to your custom target spec
target = "x86_64-boss.json"

[unstable]
json-target-spec = true
build-std = ["core", "compiler_builtins"]
build-std-features = ["compiler_builtins/mem"]

[target.'cfg(target_os = "none")']
runner = "bootimage runner"
